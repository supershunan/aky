export enum LanguageEnum {
    EN = 'en',
    CN = 'zh',
    JP = 'ja',
}

export enum MQTTStatusEnum {
    Connected = "已连接",
    Reconnecting = "重连中",
    Disconnected = "已断开",
    Error = "连接失败",
    SUBSCRIBE = '订阅'
}